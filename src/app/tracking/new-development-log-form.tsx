"use client";
import { useState } from "react";
export function NewDevelopmentLogForm({project,branch,expectedBranchCommit}:{project:string;branch:string;expectedBranchCommit:string}){const [title,setTitle]=useState(""),[details,setDetails]=useState(""),[message,setMessage]=useState<string>();async function save(e:React.FormEvent){e.preventDefault();const r=await fetch("/api/tracking/development-log",{method:"PATCH",headers:{"content-type":"application/json"},body:JSON.stringify({project,branch,expectedBranchCommit,record:{title,categories:["documentation"],reasonType:"manual_entry",reasonSummary:"Manual development-log entry",details,systemImpact:[],scope:{services:[],components:[],areas:[]},relationships:{plannedImprovementIds:[],proposalIds:[],legacyCleanupIds:[]}}})});const x=await r.json();if(!r.ok){setMessage(x.error??"Save failed.");return;}window.location.reload();}return <form className="tracking-form" onSubmit={save}><label>Title<input required value={title} onChange={e=>setTitle(e.target.value)}/></label><label>Details<textarea required value={details} onChange={e=>setDetails(e.target.value)}/></label><button>Create development log</button>{message&&<p role="status">{message}</p>}</form>}
