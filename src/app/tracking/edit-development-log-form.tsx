"use client";
import { useState } from "react";
import { developmentLogDocumentSchema } from "@/lib/tracking/schema";
type Record = typeof developmentLogDocumentSchema._output.records[number];
export function EditDevelopmentLogForm({project,branch,expectedBranchCommit,record}:{project:string;branch:string;expectedBranchCommit:string;record:Record}){const[title,setTitle]=useState(record.title),[details,setDetails]=useState(record.details),[message,setMessage]=useState<string>();async function save(e:React.FormEvent){e.preventDefault();const r=await fetch("/api/tracking/development-log",{method:"PATCH",headers:{"content-type":"application/json"},body:JSON.stringify({project,branch,expectedBranchCommit,record:{id:record.id,title,categories:record.categories,reasonType:record.reason.type,reasonSummary:record.reason.summary,details,systemImpact:record.systemImpact,scope:record.scope,relationships:record.relationships}})}),x=await r.json();if(!r.ok){setMessage(x.error??"Save failed.");return}window.location.reload()}return <details><summary>{record.id} — {record.title}</summary><form className="tracking-form" onSubmit={save}><label>Title<input required value={title} onChange={e=>setTitle(e.target.value)}/></label><label>Details<textarea required value={details} onChange={e=>setDetails(e.target.value)}/></label><button>Save development log</button>{message&&<p role="status">{message}</p>}</form></details>}
